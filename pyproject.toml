[project]
name = "pib-ml-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TalesDias", email = "dtales15@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "awscli>=1.46.0",
    "dvc[s3]>=3.67.1",
]

[project.scripts]
pib-ml-data = "pib_ml_data:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
